# Pinned to the environment the analysis was actually run in (Python 3.7.3).
# The notebook also runs unchanged on current releases; see README.

pandas==1.3.5
numpy==1.21.6
matplotlib==3.5.3
scikit-learn==1.0.2

# The word-count cell uses the classic Notebook JavaScript API (Jupyter.notebook).
# Notebook 7 is built on JupyterLab and does not expose it, so the count silently
# fails there. Keep this below 7.
notebook>=6.4,<7

# Optional: pandas warns if numexpr is older than 2.7.0. Nothing depends on it.
numexpr>=2.7.0
